#include<stdio.h> 
#include<graphics.h> 
#include<conio.h> 
void main(){ 
int gd=DETECT,gm,x,y; 
initgraph(&gd,&gm,"C:\\TURBOC3\\BGI"); 
rectangle(60,60,120,120); 
scanf("%d%d",&x,&y); 
putpixel(x,y,RED); 
if(x>=60&&x<=120&&y>=60&&y<=120) 
printf("Inside"); 
else 
printf("Outside"); 
getch(); closegraph(); 
}
